! exec count --words --lines
stderr 'one option only'
! exec count -w -l
stderr 'one option only'

! exec count --words --bytes
stderr 'one option only'
! exec count -w -b
stderr 'one option only'

! exec count --lines --bytes
stderr 'one option only'
! exec count -l -b
stderr 'one option only'
